apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: queries
spec:
  hosts:
    - queries.plantsuite.local
  gateways:
    - istio-ingress/gateway
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            port:
              number: 80
            host: queries
