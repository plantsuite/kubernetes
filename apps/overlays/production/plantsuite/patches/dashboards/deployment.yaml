apiVersion: apps/v1
kind: Deployment
metadata:
  name: dashboards
spec:
  template:
    spec:
      containers:
        - name: dashboards
          resources:
            requests:
              cpu: 200m
              memory: 512Mi
            limits:
              cpu: 200m
              memory: 512Mi