apiVersion: apps/v1
kind: Deployment
metadata:
  name: queries
spec:
  template:
    spec:
      containers:
        - name: queries
          resources:
            requests:
              cpu: 200m
              memory: 512Mi
            limits:
              cpu: 200m
              memory: 512Mi