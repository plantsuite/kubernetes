apiVersion: apps/v1
kind: Deployment
metadata:
  name: tenants
spec:
  template:
    spec:
      containers:
        - name: tenants
          resources:
            requests:
              cpu: 200m
              memory: 512Mi
            limits:
              cpu: 200m
              memory: 512Mi