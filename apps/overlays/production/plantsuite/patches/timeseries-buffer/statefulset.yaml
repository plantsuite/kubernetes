apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: timeseries-buffer
spec:
  template:
    spec:
      containers:
        - name: timeseries-buffer
          resources:
            requests:
              cpu: 200m
              memory: 512Mi
            limits:
              cpu: 200m
              memory: 512Mi