namespace: mongodb

resources:
  - bundle-v1.22.0.yaml

patches:
  - target:
      kind: Deployment
      name: percona-server-mongodb-operator
    patch: |-
      # Container: percona-server-mongodb-operator (index 0)
      - op: replace
        path: /spec/template/spec/containers/0/resources
        value:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 256Mi
