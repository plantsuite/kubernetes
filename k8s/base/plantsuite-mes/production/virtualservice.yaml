apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: production
spec:
  hosts:
    - production.plantsuite.local
  gateways:
    - istio-ingress/gateway
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            port:
              number: 8080
            host: production