apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: controlstations
spec:
  hosts:
    - controlstations.plantsuite.local
  gateways:
    - istio-ingress/gateway
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            port:
              number: 80
            host: controlstations