labels:
  - pairs:
      app: timeseries-mqtt
      app.kubernetes.io/name: timeseries-mqtt
    includeSelectors: true
    includeTemplates: true

resources:
  - hpa.yaml
  - virtualservice.yaml
  - pdb.yaml
  - service.yaml
  - statefulset.yaml

configMapGenerator:
  - name: timeseries-mqtt-appsettings
    files:
      - appsettings.json

generatorOptions:
  disableNameSuffixHash: true
