apiVersion: apps/v1
kind: Deployment
metadata:
  name: entities
spec:
  template:
    spec:
      containers:
        - name: entities
          resources:
            requests:
              cpu: 200m
              memory: 512Mi
            limits:
              cpu: 200m
              memory: 512Mi